# 实验链与证据链梳理 v3

> 本文档取代 `实验链与证据链梳理v2.md`，作为 ICUS 会议论文实验章节的直接指导文档。v3 的目标不是继续讨论材料是否充足，而是把当前论文故事落实为可写、可跑、可筛选、可控制篇幅的实验链。实验章节服务于 ICUS 论文主线：候选 gap 不是静态选择对象，而是在相邻安全余量约束下以低扰动方式协作发展，并通过扰动-效率联合评分选择发展后的最优 gap。

---

## 1. 文档定位

ICUS 论文的实验章节采用四段式结构：

```text
IV. Experiments

A. Experimental Setup
   场景、车辆参数、安全距离、评价指标、对比算法和统一比较口径。

B. Feasibility Validation
   展示 candidate gap evaluation -> selected developed gap -> trajectory execution。
   放 candidate score table 和轨迹图。

C. Capacity and Adjustment Ablation
   短实验，合并验证 adjacent-gap capacity 和 space-sensitive QP。
   放一张机制对比图/表。

D. External Algorithm Comparison in Representative Scenarios
   OneStep vs RiosTorres/FIFO。
   先跑 S01、S02、S03、S06、S07、S08 六个候选场景，再根据数据质量和论文篇幅选择正文展示场景。
```

本文不设置 `E. Discussion`。结论性解释放在各实验小节末尾和 `Conclusion` 中完成。

本文不把实验章写成完整系统报告，不引入随机车流、SUMO 闭环、多 MV rolling、内侧换道辅助 gap supply 或 CAV/HDV 混行。这些内容属于后续论文，不进入 ICUS 实验主线。

---

## 2. 实验总逻辑

ICUS 实验采用三层证据链：

```text
Feasibility Validation
-> 证明本文方法链可以从候选 gap 评价闭环执行到可行轨迹。

Capacity and Adjustment Ablation
-> 证明相邻 gap 余量约束和空间敏感 QP 的机制行为是合理的。

External Algorithm Comparison
-> 证明在代表性成功合流场景中，OneStep 相比 RiosTorres/FIFO 可以保持相近物理合流效率，同时减少主线扰动。
```

这三层证据分别对应三个审稿人最可能追问的问题：

| 审稿人问题 | 对应实验 | 主文回答 |
|---|---|---|
| 方法链能不能真的从 gap 评价走到轨迹执行？ | B. Feasibility Validation | candidate score table + selected gap execution + MV trajectory |
| adjacent-gap capacity 和 space-sensitive QP 是否只是公式包装？ | C. Capacity and Adjustment Ablation | 四个机制 case 显示算法会按空间余量分配调整，并避免无必要协作 |
| 相比已有 FIFO/RiosTorres 类协调方法，本文方法是否真的低扰动？ | D. External Algorithm Comparison | 统一物理入口/出口时间下比较主线受影响车数、加速度能量和恢复时间 |

---

## 3. Claim Register

实验章只支撑以下有限而清晰的 claims。

| Claim ID | 论文主张 | 支撑实验 | 可写强度 |
|---|---|---|---|
| C1 | 候选 gap 可以被建模为 developable traffic resource，而不是只按当前长度静态选择。 | B | 强。通过 candidate gap score table 展示每个 gap 的 shortage、capacity、cooperation cost、ego cost 和 final score。 |
| C2 | 被选中的 developed gap 可以进一步转化为合流时间、合流位置和可行 MV 轨迹。 | B | 强。通过轨迹、速度、加速度和终端状态展示闭环执行。 |
| C3 | 相邻 gap 余量可以作为边界车辆协作能力的状态相关约束。 | C | 中强。通过前后余量变化 case 证明调整方向和可用余量一致。 |
| C4 | space-sensitive QP 会把调整负担从空间紧张侧转移到空间充足侧。 | C | 中强。通过 equal split / proposed 对比和 S06/S07/S08 机制 case 支撑。 |
| C5 | 在代表性成功合流场景中，OneStep 能在保持相近物理合流时间的同时减少主线扰动。 | D | 中强。限定为 representative scenarios，不写成全局最优或所有交通条件下优于 FIFO。 |

实验章不支撑以下 claims：

```text
OneStep 在所有场景中都优于 FIFO。
OneStep 全局最小化交通扰动。
本文方法已经解决密集主线无 gap 可调时的 gap supply 问题。
本文方法已经验证随机车流、SUMO 闭环、多 MV rolling 或 CAV/HDV 混行。
```

---

## 4. A. Experimental Setup

### 4.1 场景设置

实验采用单帧局部匝道合流决策场景。主线车辆在目标车道上给定纵向位置，MV 根据当前状态评价候选 mainline gaps。每个候选 gap 由后边界车和前边界车定义。

核心设置沿用当前算法文档与 S01 对比报告：

```text
candidate gap length: G_i
required merging gap length: G_req
gap shortage: Delta_i = max(0, G_req - G_i)
front-boundary adjustment: delta_f
rear-boundary adjustment: delta_r
front-side capacity: delta_f_bar
rear-side capacity: delta_r_bar
mainline cooperation cost: C_coop
MV ego merging cost: C_ego
developed-gap score: J_i
```

当前测试用例中 `G_req = 85 m`。相邻安全阈值和车辆动力学约束按代码与实验配置统一设置，论文中只列对实验解释有必要的参数，不展开成系统参数表。

### 4.2 对比算法

外部对比使用：

| 方法 | 含义 |
|---|---|
| OneStep | 本文方法。先评价候选 gap，选择 developed gap，再只对 MV 和选中 gap 的边界车辆生成协作轨迹。 |
| RiosTorres/FIFO | FIFO 队列式协调 baseline。按车辆接近合流区域的顺序生成通过合流区的控制轨迹。 |

内部机制对比使用：

| 方法 | 含义 | 用途 |
|---|---|---|
| Equal split | 将 `Delta_i` 平均分配给前后边界车 | 检查 proposed QP 是否能避免机械平均分配 |
| Unweighted QP | 最小化未加空间权重的二次调整量 | 检查空间敏感权重的作用 |
| Proposed | 使用相邻 gap capacity 和 space-sensitive QP | 本文机制 |

主文 C 节优先使用 `Equal split` 与 `Proposed` 对比。如果表格空间允许，再加入 `Unweighted QP`。`Capacity-proportional` 可作为补充实验或备选 baseline，不作为 ICUS 主文必需项。

### 4.3 统一物理比较口径

外部算法对比不能直接比较各算法内部的 `algorithm_merge_point`，因为 OneStep 的算法合流点是选中 gap 的中心合流位置，而 RiosTorres/FIFO 使用统一物理合流区入口。

外部对比统一使用：

```text
physical merge entry: x = 555 m
physical merge exit:  x = 600 m
```

核心效率指标：

```text
MV physical merge entry time
MV physical merge exit time
```

核心主线扰动指标：

```text
affected mainline vehicle count
mainline acceleration energy
mainline speed recovery time
```

安全解释指标：

```text
minimum spacing
minimum TTC
```

辅助解释指标：

```text
mainline average speed
MV acceleration energy
MV maximum speed
```

`mainline average speed` 不作为低扰动主张的主指标。若 FIFO 平均速度更高，需要与 `mainline acceleration energy` 和 `affected mainline vehicle count` 同时解释：更高平均速度可能来自更大范围的主线加速，不等于更低扰动。

---

## 5. B. Feasibility Validation

### 5.1 实验定位

`Feasibility Validation` 是本文实验章的第一层证据。它证明本文方法不是停留在 gap score 或静态 gap selection，而是可以形成完整规划链：

```text
candidate gaps
-> adjacent-gap-preserving cooperation capacity
-> candidate-level minimum adjustment estimate
-> disturbance-efficiency valuation
-> selected developed gap
-> executed boundary adjustment
-> terminal time/location
-> feasible MV trajectory
```

### 5.2 推荐场景

Feasibility Validation 使用 S01 原始基准场景：

```text
X = [-180, -90, -25, 30, 110, 190, 250]
expected best_gap = [30, 110]
expected delta_f = 5
expected delta_r = 0
expected d = 72.5
expected t_m ≈ 13.594
expected p_m ≈ 344.375
```

S01 的作用是展示完整链条，而不是承担所有机制消融和所有外部算法比较。

### 5.3 主文图表

Feasibility Validation 使用一张 candidate score table 和一张轨迹图。

**Table: Candidate Gap Evaluation and Selection**

| Gap | `G_i` | `Delta_i` | `delta_f_bar` | `delta_r_bar` | `delta_f*` | `delta_r*` | `C_coop` | `C_ego` | `J_i` | Decision |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| gap 1 |  |  |  |  |  |  |  |  |  | feasible / infeasible / selected |
| gap 2 |  |  |  |  |  |  |  |  |  | feasible / infeasible / selected |
| gap 3 |  |  |  |  |  |  |  |  |  | feasible / infeasible / selected |

该表的写作功能：

```text
1. 证明本文不是只看当前 gap 长度；
2. 证明当前不足的 gap 可以经过有限协作发展；
3. 证明最终选择来自 C_coop 和 C_ego 的联合评分；
4. 证明候选级估计和最终执行是两个不同动作。
```

**Figure: Feasibility and Trajectory Execution**

图中包含：

```text
(a) candidate gaps before cooperation and selected developed gap after adjustment
(b) MV longitudinal trajectory x(t), with terminal time and terminal position marked
(c) optional: speed and acceleration profiles, if space permits
```

若篇幅不足，速度和加速度曲线只保留在补充分析中，主文保留 `(a)` 和 `(b)`。

### 5.4 主文结论句

中文逻辑：

> S01 表明，本文方法能够对多个候选 gap 计算协作能力、最小必要调整量和扰动-效率评分，并选择 `[30,110]` 作为 developed gap。选中 gap 的边界调整进一步确定 MV 的合流时间和合流位置，使方法链能够从 candidate gap evaluation 闭环执行到可行合流轨迹。

英文写作句：

> The S01 case demonstrates that the proposed planner forms an executable chain from candidate-gap evaluation and developed-gap selection to terminal condition determination and feasible MV trajectory generation.

---

## 6. C. Capacity and Adjustment Ablation

### 6.1 实验定位

`Capacity and Adjustment Ablation` 是本文实验章的第二层证据。它合并验证两个机制：

```text
adjacent-gap-preserving cooperation capacity
space-sensitive minimum-disturbance adjustment
```

该实验不追求外部算法性能对比，也不使用“算法无法合流”的失败场景作为主文证据。它只证明本文机制具有以下行为：

```text
1. 当目标 gap 已经满足安全长度时，不产生无意义主线协作；
2. 当前后相邻余量对称时，调整量自然接近对称分配；
3. 当前方相邻余量更大时，优先让前边界车前移；
4. 当后方相邻余量更大时，优先让后边界车后移。
```

### 6.2 四个机制 case

主文 C 节采用四个 case。它们与外部对比候选场景共享，避免额外制造一套不一致实验。

| Case | 对应场景 | 机制现象 | 预期结果 | 主要证明 |
|---|---|---|---|---|
| C1 | S02 | 目标 gap 已满足安全长度 | `Delta=0`, `delta_f=0`, `delta_r=0` | 本文方法不会为了优化评分而无意义扰动主线 |
| C2 | S06 | 前后相邻余量相等 | `delta_f ≈ delta_r` | space-sensitive QP 在对称空间下给出近似对称调整 |
| C3 | S07 | 前方相邻空间更大 | `delta_f > delta_r`, 典型为 `delta_f=5`, `delta_r=0` | 调整负担转向前方空间充足侧 |
| C4 | S08 | 后方相邻空间更大 | `delta_r > delta_f`, 典型为 `delta_f=0`, `delta_r=5` | 调整负担转向后方空间充足侧 |

当前测试用例中 S01 与 S07 的位置配置可能重合。跑实验时必须执行场景去重规则：

```text
若 S01 与 S07 完全相同，则 S07 保留为 mechanism-label case，或将 S07 改造成非重复的 front-side margin larger variant。
最终论文中不得把完全重复的 S01/S07 当成两个独立证据点。
```

### 6.3 内部 baseline

C 节 baseline 只服务机制解释，不扩展成大规模消融。

主文保留：

```text
Equal split
Proposed
```

可选保留：

```text
Unweighted QP
```

不在主文展开：

```text
Capacity-proportional
front-only / rear-only
full valuation baseline
parameter sensitivity
```

### 6.4 指标

C 节主文指标控制在一张表内：

| Metric | 含义 | 写作作用 |
|---|---|---|
| `Delta_i` | 当前 gap shortage | 说明需要补足多少 gap |
| `delta_f*`, `delta_r*` | proposed adjustment | 展示调整方向与大小 |
| `delta_f_eq`, `delta_r_eq` | equal split adjustment | 展示朴素平均分配 |
| tight-side margin after adjustment | 紧张侧调整后剩余余量 | 说明 proposed 是否保护紧张侧空间 |
| minimum neighboring-gap margin | 调整后最小相邻安全余量 | 说明没有把风险转嫁给相邻 gap |

`C_coop` 可以作为附加列。若表格过宽，`C_coop` 不进主文，只在正文中用一句话解释 proposed 的低扰动分配来自空间敏感二次代价。

### 6.5 主文图表

**Table: Capacity and Adjustment Ablation**

| Case | Scenario | `Delta` | Neighboring margins | Equal split `delta_f/delta_r` | Proposed `delta_f/delta_r` | Min margin after proposed | Mechanism verified |
|---|---|---:|---|---|---|---:|---|
| C1 | S02 | 0 | sufficient | 0 / 0 | 0 / 0 |  | no unnecessary cooperation |
| C2 | S06 | 20 | symmetric | 10 / 10 | ≈10 / ≈10 |  | symmetric allocation |
| C3 | S07 | 5 | front-side larger | 2.5 / 2.5 | 5 / 0 |  | front-side preference |
| C4 | S08 | 5 | rear-side larger | 2.5 / 2.5 | 0 / 5 |  | rear-side preference |

这张表比多张图更适合 ICUS 篇幅。若画图，则只画一个小型 4-case bar chart，横轴为 case，纵轴为 `delta_f` 和 `delta_r`，用并列柱显示 proposed 的调整方向。

### 6.6 主文结论句

中文逻辑：

> 机制消融表明，本文方法不会在 `Delta=0` 时产生无必要协作；当前后相邻余量对称时，调整近似平均分配；当一侧相邻余量更充足时，space-sensitive QP 会将主要调整量分配给该侧边界车辆，从而减少对紧张侧相邻 gap 的消耗。

英文写作句：

> The ablation cases show that the proposed adjustment model does not introduce unnecessary cooperation when the target gap is already safe, and it shifts the required adjustment toward the boundary vehicle with larger neighboring-gap margin.

---

## 7. D. External Algorithm Comparison in Representative Scenarios

### 7.1 实验定位

`External Algorithm Comparison` 是本文实验章的第三层证据。它比较 OneStep 与 RiosTorres/FIFO 在代表性成功合流场景中的表现。

该实验的目标是证明：

```text
在统一物理合流入口/出口时间近似相同的情况下，
OneStep 可以减少受影响主线车辆数、主线加速度能量和扰动恢复时间。
```

该实验不承担以下任务：

```text
证明 capacity constraint 在合流失败场景中不会强行构造 gap；
证明所有主线密集场景都能被本文方法解决；
证明本文方法已经覆盖 lane-change-assisted gap supply。
```

因此，D 节不纳入 S05 这类 OneStep 可能无法合流的密集不可行场景。合流失败或协作不可行场景可以作为代码测试或后续论文的 gap-supply 问题，不作为 ICUS 外部性能对比主证据。

### 7.2 六个候选场景

D 节先跑六个候选场景：

| Scenario | 场景作用 | 预期 OneStep 行为 | 写作功能 |
|---|---|---|---|
| S01 | 原始基准场景 | 选择 `[30,110]`，局部调整前边界车 | 主场景，已有 S01 报告支撑，展示低扰动优势 |
| S02 | 已有安全 gap / 无需协作 | `Delta=0`, 不调整主线 | control case，证明本文方法不会无意义扰动主线 |
| S03 | 可用 gap 在 MV 后方 | `d_i<0`，MV 减速等待后方 gap | 展示方法不只处理前方追赶型合流，也能处理等待型合流 |
| S06 | 前后余量对称 | `delta_f≈delta_r` | 展示外部闭环中的对称协作行为 |
| S07 | front-side margin larger | `delta_f>delta_r` | 展示前方空间充足时的前边界车优先调整 |
| S08 | rear-side margin larger | `delta_r>delta_f` | 展示后方空间充足时的后边界车优先调整 |

这六个场景同时覆盖 B、C、D 三个实验小节所需证据：

```text
S01 -> Feasibility Validation + primary external comparison
S02 -> no unnecessary cooperation
S03 -> negative terminal displacement / waiting behavior
S06 -> symmetric adjustment
S07 -> front-side preference
S08 -> rear-side preference
```

### 7.3 正文场景选择规则

跑实验阶段保留六个场景。写论文时正文不必全部展示。

正文展示场景按以下规则选择：

```text
1. S01 必须保留。
2. 其余场景从 S02、S03、S06、S07、S08 中选择 2 个或 3 个。
3. 被写入正文的场景必须满足 OneStep 与 FIFO 都完成物理合流。
4. 被写入正文的场景应覆盖不同机制，不写重复场景。
5. 若 S01 与 S07 完全重复，则正文只保留其中一个。
6. 最终选择不能只按结果最漂亮，而要按“数据有效 + 机制覆盖 + 低扰动结论清楚”三条标准。
```

推荐正文组合优先级：

```text
组合 A：S01 + S02 + S03
用途：主场景 + 无需协作 control + 后方 gap 等待型合流。

组合 B：S01 + S06 + S08
用途：主场景 + 对称调整 + 后方空间优先调整。

组合 C：S01 + S02 + S06 + S08
用途：若篇幅允许，用四个场景更完整覆盖 no-adjustment、symmetric、directional adjustment。
```

最终组合以真实数据质量为准。正文只写能清楚支撑低扰动主线的场景；不把异常、重复或解释负担过大的场景硬塞进主文。

### 7.4 外部对比指标

D 节主表使用以下指标：

| 指标 | OneStep 预期优势 | 解释 |
|---|---|---|
| `physical_merge_entry_time` | 相近或略优 | 统一物理入口时间，说明合流效率没有明显损失 |
| `physical_merge_exit_time` | 相近或略优 | 统一物理出口时间，避免内部 merge point 语义差异 |
| `affected_mainline_vehicle_count` | 更低 | 说明扰动范围更小 |
| `mainline_acceleration_energy` | 更低 | 说明主线控制强度更小 |
| `mainline_speed_recovery_time` | 更短 | 说明扰动持续时间更短 |
| `min_spacing` | 不低于安全阈值，最好更大 | 安全裕度解释指标 |
| `min_ttc` | 不低于安全阈值，最好更大 | closing risk 解释指标 |

辅助指标：

| 指标 | 解释方式 |
|---|---|
| `mainline_average_speed` | FIFO 可能更高，但必须结合主线加速度能量和受影响车数解释 |
| `MV acceleration energy` | FIFO 可能更平滑，但这是以更大范围主线调速为代价 |
| `MV maximum speed` | 用于解释 MV 自车代价，不作为本文主线扰动 claim 的主证据 |

### 7.5 S01 已有结果写入方式

S01 当前已有对比报告，核心结果如下：

| 指标 | OneStep | RiosTorres/FIFO | 论文解释 |
|---|---:|---:|---|
| MV physical entry time | `24.125s` | `24.25s` | OneStep 略快 `0.125s` |
| MV physical exit time | `26.375s` | `26.5s` | OneStep 略快 `0.125s` |
| affected mainline vehicles | `1` | `6` | OneStep 扰动范围更小 |
| mainline acceleration energy | `0.1706` | `22.1881` | OneStep 主线控制强度更低 |
| recovery time | `13.1s` | `31.0s` | OneStep 扰动持续时间更短 |
| min spacing | `55.0m` | `45.0m` | OneStep 安全裕度更大 |
| min TTC | `112.32s` | `22.31s` | OneStep closing risk 更低 |

S01 的英文结论句：

> In S01, both methods complete the physical merge with nearly identical entry and exit times. However, OneStep affects only one mainline vehicle, whereas FIFO affects six; OneStep also reduces the mainline acceleration energy from 22.1881 to 0.1706 and shortens the recovery time from 31.0 s to 13.1 s.

### 7.6 主文图表

**Table: External Comparison Across Representative Scenarios**

| Scenario | Method | Entry time | Exit time | Affected vehicles | Mainline acc. energy | Recovery time | Min spacing | Min TTC |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| S01 | OneStep |  |  |  |  |  |  |  |
| S01 | FIFO |  |  |  |  |  |  |  |
| S02/S03/... | OneStep |  |  |  |  |  |  |  |
| S02/S03/... | FIFO |  |  |  |  |  |  |  |

若表格过宽，安全指标 `min spacing` 和 `min TTC` 合并成一列 `Safety margin`，或只在正文中解释。

**Figure: Disturbance Comparison**

可选画一个 compact grouped bar chart：

```text
x-axis: scenarios
bars: affected vehicle count, mainline acceleration energy, recovery time
methods: OneStep vs FIFO
```

若页数紧张，D 节只放主表，不放图。S01 的 time-space 对比图可作为 B 或 D 的辅助图，但主文最多保留一张轨迹类图。

### 7.7 主文结论句

中文逻辑：

> 在代表性成功合流场景中，OneStep 与 RiosTorres/FIFO 均能完成物理合流，且入口/出口时间保持相近。与 FIFO 相比，OneStep 的主要优势不是提高主线平均速度，而是通过更局部的边界车辆协作减少受影响主线车辆数、主线加速度能量和扰动恢复时间。

英文写作句：

> Across representative successful merging scenarios, OneStep preserves comparable physical merge timing while reducing the spatial extent, control intensity, and duration of mainline disturbance relative to the FIFO baseline.

---

## 8. 实验章节页数与图表预算

ICUS full paper 最好控制在 5-6 页。实验章目标控制在约 1.5-2 页。

| 小节 | 页数预算 | 图表预算 |
|---|---:|---|
| A. Experimental Setup | 0.25-0.35 页 | 无独立大表，参数写进正文 |
| B. Feasibility Validation | 0.45-0.60 页 | 1 个 candidate score table + 1 个轨迹图 |
| C. Capacity and Adjustment Ablation | 0.35-0.50 页 | 1 个机制表 |
| D. External Algorithm Comparison | 0.55-0.75 页 | 1 个跨场景对比表，可选 1 个小图 |

主文实验图表上限：

```text
Table I: Candidate gap evaluation and selection
Figure 1 or 2: Feasibility / trajectory execution
Table II: Capacity and adjustment ablation
Table III: External algorithm comparison
```

如果篇幅不足，优先保留：

```text
1. Candidate score table
2. Capacity and adjustment ablation table
3. External comparison table
```

轨迹图可以压缩为单栏图，或与方法示意图合并处理。

---

## 9. 数据产物清单

每个外部对比场景需要整理以下文件或等价数据：

```text
summary.json
metrics.csv
comparison_trajectory.csv
time-space plot
mainline speed / acceleration profile
```

D 节六个候选场景：

```text
S01
S02
S03
S06
S07
S08
```

C 节机制表需要整理：

```text
scenario id
target gap
G_i
Delta_i
front/rear neighboring margin
equal split delta_f/delta_r
proposed delta_f/delta_r
minimum neighboring-gap margin after adjustment
C_coop, if available
```

B 节 S01 candidate score table 需要整理：

```text
all candidate gaps
G_i
Delta_i
delta_f_bar
delta_r_bar
delta_f*
delta_r*
C_coop
C_ego
J_i
selected / not selected / infeasible
```

---

## 10. 与 Method 和 Introduction 的对齐

### 10.1 Method 对齐

Method 中各模块与实验对应如下：

| Method 模块 | 对应实验 | 写作边界 |
|---|---|---|
| Problem Formulation | B + D | 说明主线协作有代价，不能写成全局交通最优问题 |
| Adjacent-Gap-Preserving Cooperation Capacity | C | 强调相邻余量决定可调整方向与容量 |
| Space-Sensitive Minimum-Disturbance Adjustment | C | 强调调整负担随空间余量自动分配 |
| Disturbance-Efficiency Valuation | B | 用 candidate score table 展示评分与选择 |
| Terminal Time/Location and Trajectory | B | 用 S01 轨迹图展示可执行性 |
| Overall Low-Disturbance Effect | D | 用 OneStep vs FIFO 支撑外部效果 |

Method 中不得把 C 节写成“完整系统消融已经证明所有模块最优”。正确写法是：

```text
The ablation cases verify the intended behavior of the proposed capacity and adjustment mechanisms.
```

### 10.2 Introduction 对齐

Introduction 中的实验钩子改成三层：

```text
First, a feasibility case demonstrates that the proposed chain can evaluate candidate gaps, select the best developed gap, and generate an executable merging trajectory.

Second, compact ablation cases verify that the proposed capacity and adjustment mechanisms allocate cooperation according to neighboring-gap margins.

Third, representative comparisons with a FIFO baseline show that the proposed method preserves comparable physical merging time while reducing mainline disturbance.
```

Introduction 不写：

```text
large-scale statistical validation
random traffic validation
SUMO closed-loop validation
mixed CAV/HDV validation
```

---

## 11. 当前版本最终判断

v3 的实验链是 ICUS 论文的主文实验方案。

最终实验章由以下三类证据组成：

```text
1. S01 Feasibility Validation
2. Four-case Capacity and Adjustment Ablation using S02/S06/S07/S08
3. OneStep vs RiosTorres/FIFO comparison over S01/S02/S03/S06/S07/S08 candidate pool
```

写论文时，正文不需要展示所有六个外部对比场景。实验先全部跑，主文根据数据质量、机制覆盖和篇幅选择 3-4 个场景。S01 必须保留；重复场景、解释负担过重场景和无法完成合流的场景不进入 D 节主文。

这套设计能够支撑 ICUS 论文的核心论断：

> 本文提出的 mainline-disturbance-aware cooperative gap development 方法可以把候选 gap 评价、空间余量约束、最小扰动协作调整、扰动-效率联合评分和可行轨迹生成连接成一条可执行链，并在代表性成功合流场景中相较 FIFO 类协调方法减少主线扰动。

